---
import { getCollection } from 'astro:content'
import BlogIndex from '~/layouts/BlogIndex.astro'
import { postsForLang } from '~/data/blog'

const posts = postsForLang(await getCollection('blog'), 'en')
---

<BlogIndex lang="en" posts={posts} />
